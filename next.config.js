/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = {
  i18n: {
    locales: ['ar-EG'],
    defaultLocale: 'ar-EG',
  },
}

module.exports = nextConfig
